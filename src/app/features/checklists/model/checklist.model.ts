export class ChecklistModel {
  constructor(
    public id: string,
    public name: string,
    public x: number,
    public y: number,
    public color: number,
    public height: number,
    public width: number
  ) {
  }
}
